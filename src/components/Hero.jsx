import React from "react";
import { SectionWrapper } from "../hoc";

const Hero = () => {
    return (
        <div className="flex">
            <h1>Hero</h1>
        </div>
    )
}

export default SectionWrapper(Hero, "")